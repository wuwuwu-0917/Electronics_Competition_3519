# 2026年TI杯全国大学生电子设计竞赛
# H题：车载平衡滚球运动控制系统

2026年8月1日

---

## 摘要

本设计实现了一套车载平衡滚球运动控制系统，由循迹小车、摆杆平衡装置和视觉检测模块三部分组成。小车采用MSPM0G3519微控制器作为主控芯片，通过6路灰度光电管实现黑线循迹，配合增量式PID速度环与位置式PID方向环构成双闭环控制；摆杆端采用MaixCAM2 AI摄像头运行YOLOv5目标检测模型实时识别钢球位置，结合透视矫正与Kalman追踪器输出精确的厘米级偏差，通过双模式PD控制器直驱舵机调整摆杆倾角，实现钢球的动态平衡。系统同时具备Wi-Fi MJPEG实时推流、设备端录像回放、触摸屏模式切换等功能，满足赛题全部要求。

**关键词**：平衡滚球 循迹小车 YOLOv5 Kalman追踪 PID控制 透视矫正

---

## 目录

一、系统方案论证
　　1. 小车循环控制方案
　　2. 摆杆平衡系统方案
　　3. 视觉检测与追踪方案
二、理论分析与计算
　　1. 摆杆运动建模与平衡控制原理
　　2. 透视矫正与坐标映射
　　3. Kalman滤波与追踪预测
三、电路与程序设计
　　1. 硬件电路设计
　　2. 软件程序设计
四、测试方案与测试结果
　　1. 测试方案
　　2. 测试结果

---

## 一、系统方案论证

### 1. 小车循环控制方案

#### (1) 整体方案选择

小车需要在给定的黑色轨迹线上自主循迹行驶。综合考虑成本、可靠性和开发周期，本设计选用**6路灰度光电管**作为循迹传感器，相较于摄像头循迹方案具有处理简单、不受环境光干扰、响应速度快等优势。

**方案对比：**

| 方案 | 优点 | 缺点 | 结论 |
|------|------|------|------|
| 摄像头循迹 | 信息丰富、前瞻距离远 | 处理复杂、对MCU性能要求高 | 不选用 |
| 灰度光电管 | 响应快、抗干扰、成本低 | 信息量有限 | **选用** |
| 电磁循迹 | 不受光线影响 | 需铺设导线、改造赛道 | 不选用 |

#### (2) 电机控制方案

小车采用两个直流减速电机驱动后轮，前轮为万向轮。电机控制采用**双闭环PID架构**：
- **内环（速度环）**：增量式PID，分别控制左、右电机转速，目标速度20~30 rpm
- **外环（方向环）**：位置式PID，以车头偏离黑线的误差为输入，输出转向修正量叠加到速度环目标值

方向环输出经**5点滑动平均滤波**后叠加，有效抑制了传感器噪声引起的转向抖动，提高了直线行驶的稳定性。

**速度环PID参数**（增量式）：KP=7.0, KI=0.2, KD=0, 输出限幅±100

**方向环PID参数**（位置式）：KP=1.0(标准)/1.3(高速), KI=0, KD=0.1, 输出限幅±100

#### (3) 差速转向模型

6路灰度传感器以二值化信号加权求和计算转向误差：

```
偏差 = 8*(!S0) + 4*(!S1) + 1*(!S2) + (-1)*(!S3) + (-4)*(!S4) + (-8)*(!S5)
```

其中黑线返回0、白线返回1（传感器逻辑），偏差>0表示车偏左需右转，偏差<0表示车偏右需左转。方向环PID将该偏差调节至0（车居中于黑线），输出以相反极性叠加到左右电机速度目标值，实现差速转向。

#### (4) 停车线检测方案

采用**上升沿检测**方式：当6路光电管中>=3路同时检测到黑色且上一周期不满足此条件时，记为一次停车线事件。累计检测到第2条停车线时触发停车，同时冻结计时器。该方法有效避免重复计数和误触发。

#### (5) 双速模式

按键2设定目标速度30rpm（高速模式），按键3设定20rpm（低速模式）。高速模式下方向环KP从1.0增大到1.3，以提供更强的转向能力适应更高车速。

### 2. 摆杆平衡系统方案

#### (1) 舵机直驱方案论证

摆杆倾斜控制采用**MaixCAM2直驱舵机**方式（视觉模块直接输出PWM），无需经过小车MCU中转：

| 方案 | 架构 | 延迟 | 控制算法复杂度 | 选用 |
|------|------|------|---------------|------|
| MCU中转控制 | 摄像头→UART→MCU→PWM→舵机 | 2~3帧(>30ms) | 受限于MCU性能 | 不选用 |
| **视觉直驱** | 摄像头→PWM→舵机 | <1帧(<16ms) | Python灵活实现 | **选用** |

此外，视觉直驱方案中UART仅需单向发送位置数据给MCU用于屏幕显示和调试，简化了通信架构。

#### (2) 双模式PD控制策略

针对钢球平衡中**稳态精细保持**与**扰动快速恢复**这对矛盾需求，创新性地设计了**双模式PD控制器**：

| 参数 | 稳态模式(S) | 扰动模式(D) | 说明 |
|------|------------|------------|------|
| KP | 8.0 | 10.0 | 位置比例增益 (deg/cm) |
| KD | 4.0 | 7.0 | 速度微分增益 (deg·s/cm) |
| 死区 | ±0.2cm | 0 | P项死区，防稳态振荡 |
| D上限 | ±40° | ±60° | D项幅值钳制 |
| 舵机角速率 | 300°/s | 600°/s | 输出变化率限制 |
| 进入扰动条件 | — | |err|>2.5cm 或 |v|>10cm/s | — |
| 退出扰动条件 | |err|<1.5cm 且 |v|<5cm/s | — | — |

**切换逻辑**采用**宽滞后策略**（进入阈值>退出阈值），防止在边界附近频繁切换模式造成抖动。扰动模式下仅使用PD（I项清零），避免积分助推过冲；稳态模式下启用慢速I项纠正微小稳态偏移。

#### (3) Kalman速度获取

D项使用Kalman追踪器估计的钢球运动速度（零滞后），相比传统差分求速度方案：
- 无滞后延迟（差分需历史数据，天然滞后至少1帧）
- 自动滤除检测噪声（Kalman本身即最优估计器）
- 更平滑稳定的速度信号

### 3. 视觉检测与追踪方案

#### (1) 整体视觉流程

视觉系统完成以下处理流水线：

```
摄像头采集 → 绿色管道检测(首次) → 透视矫正 → YOLOv5推理 → Kalman追踪+预测
    ↓                                                         ↓
  原图显示                                                UART发送+舵机控制
```

**核心设计原则**：YOLO在原始图像上推理（保持模型精度），透视矫正图仅用于坐标映射和显示参考线。

#### (2) 绿色管道透视矫正（仅首次执行）

上电后利用LAB色彩空间进行色块检测（`find_blobs`），提取绿色管道区域四个角点，经排序得到[左上, 右上, 右下, 左下]后进行一次透视变换（`image.perspective`），将倾斜管道铺平为俯视矩形。

绿色阈值：L:(0,100), A:(-128,-36), B:(-128,127)；最小色块面积150像素。

只检测一次是因为摄像头与管道相对位置固定不变，反复检测会增加不必要的计算开销。

#### (3) YOLOv5目标检测

自训练YOLOv5模型（`model_310357.mud`）在MaixCAM2 NPU上推理。

| 参数 | 值 |
|------|-----|
| 置信度阈值 | 0.4 |
| IoU NMS阈值 | 0.45 |
| 推理设备 | MaixCAM2 NPU |
| 输入分辨率 | 模型原生分辨率 |

**关键设计**：检测到多个目标时仅保留置信度最高的一个（单球场景），避免假阳性干扰。

#### (4) Kalman追踪器

采用两个独立的一维Kalman滤波器（x、y方向解耦），状态向量为[位置, 速度]。配合贪心匹配（匹配门限80px）、coast机制（最多15帧丢帧保持）和前向预测（1帧补偿NPU延迟）实现稳定追踪。

**防过冲钳制**：预测值不超过最新检测位置±半个球宽，确保急停/急转时预测值在0.5~1帧内拉回真实位置。

#### (5) 双线性透视点映射

由于YOLO在原图上检测，需将球心坐标映射到warp图以计算cm偏差。对单点进行双线性插值：

```python
u = (px - left_x) / (right_x - left_x)    # 水平比例
v = (py - top_y) / (bot_y - top_y)         # 垂直比例
warp_x = u * (warp_w - 1)                  # warp图x坐标
```

该映射等价于`image.perspective()`的逆运算，避免对整图做YOLO推理带来的尺寸不匹配问题。

#### (6) 位置换算与UART通信

```python
ball_cm = (warp_x - W/2) * PIPE_LENGTH / W
# 例如：W=224, PIPE_LENGTH=25cm => 分辨率约0.11cm/px
```

UART通信协议（115200bps，CRC16-CCITT校验）：

```
帧格式: AA 55 | pos_cm(2B signed LE * 100) | CRC16(2B LE) | DD
帧长: 7字节
```

---

## 二、理论分析与计算

### 1. 摆杆运动建模与平衡控制原理

#### (1) 物理模型

摆杆绕支点转动，钢球沿管道滚动。忽略钢球与管壁的摩擦，钢球的运动由重力沿管道方向的分量和摆杆角加速度共同决定：

```
钢球加速度 ≈ g·sinθ + α·r
```
其中θ为摆杆倾斜角，α为摆杆角加速度，r为钢球偏离支点的距离。

在小角度近似下（θ<15°），`sinθ≈θ(弧度)`，系统可近似为线性。

#### (2) PD控制原理

**P项**（比例控制）：`KP * error`，提供与偏差成正比的恢复力。增大KP加快响应但会引起超调和振荡。

**D项**（微分控制）：`KD * velocity`，抑制速度变化率，提供\"预测性刹车\"的效果。D项使用Kalman速度（零滞后），能在钢球接近中心前提前减速，防止过冲。

**死区**：稳态模式下设置±0.2cm死区，P项在死区内输出0。避免机械间隙和传感器噪声引起的\"极限环\"微振。

#### (3) 双模式切换理论

钢球系统是一个**条件稳定系统**：
- 小偏差时，线性近似成立，精细PD即可收敛
- 大偏差时，非线性效应显著，需要更强的控制力

双模切换的**宽滞后设计**（进入/退出阈值不对称）基于Schmitt触发器原理，防止噪触发边界振荡：

```
进入扰动: |err| > 2.5cm OR |v| > 10cm/s
退出扰动: |err| < 1.5cm AND |v| < 5cm/s
```

#### (4) 舵机角度与管道倾角的关系

舵机角90°对应管道水平（零倾角），0°对应管道最左倾，180°对应最右倾。输出限幅[30°, 150°]对应最大倾角约±60°。

中间位置分辨率：`0.11°舵机角 ≈ 0.002 rad倾角`，足够精细。

### 2. 透视矫正与坐标映射

#### (1) 透视变换的数学模型

给定原图四角点(TL, TR, BR, BL)和目标矩形(0,0)-(W,dst_h)，透视变换矩阵H满足：

```
[x' y' w'] = [x y 1] · H
warp_x = x'/w', warp_y = y'/w'
```

MaixPy的`image.perspective()`内部使用OpenCV的`getPerspectiveTransform`+`warpPerspective`，硬件加速实现。

#### (2) 逆映射（点对点）

对于单点(px, py)到warp图的映射，使用双线性插值等价于解以下比例关系：

在水平位置px处：
```
top_y(px) = TL.y + (TR.y - TL.y) * (px - TL.x) / (TR.x - TL.x)
bot_y(px) = BL.y + (BR.y - BL.y) * (px - BL.x) / (BR.x - BL.x)
v = (py - top_y) / (bot_y - top_y)
```

在垂直位置py处：
```
left_x(py) = TL.x + (BL.x - TL.x) * (py - TL.y) / (BL.y - TL.y)
right_x(py) = TR.x + (BR.x - TR.x) * (py - TR.y) / (BR.y - TR.y)
u = (px - left_x) / (right_x - left_x)
```

得到的(u,v)即为warp图的归一化坐标。此方法精度取决于管道的平直程度和绿色管道的检测精度，实测误差<3px，对应<0.3cm。

### 3. Kalman滤波与追踪预测

#### (1) 一维Kalman滤波器推导

状态向量：`x = [pos, vel]^T`

**预测步**（恒速模型）：
```
pos_pred = pos + vel    （假设dt=1帧）
P_pred = F·P·F^T + Q
```
其中F=[[1,1],[0,1]]，Q为过程噪声。

**更新步**：
```
K = P_pred·H^T / (H·P_pred·H^T + R)    // Kalman增益
pos = pos_pred + K*(measurement - pos_pred)
vel = vel_pred + K*(measurement - pos_pred)
P = (I - K·H)·P_pred
```
其中H=[1,0]（只观测位置），R为测量噪声协方差。

#### (2) 前向预测的合理性

NPU推理存在约1帧延迟（~16ms @ 60FPS）。Kalman前向预测1帧恰好补偿该延迟：

```
predicted_pos = kalman_pos + kalman_vel * 1
```

这使得控制器获得的偏差值比裸检测值超前约1帧，等效于消除了NPU推理延迟。

#### (3) 防过冲钳制数学分析

运动方向突然改变时（如钢球碰壁反弹），前向预测会产生错误的方向估计。钳制机制：

```
|predicted - last_detection| <= max(ball_width * 0.6, 5.0) px
```

即预测位置不超过最近检测位置±半个球宽。该约束确保方向改变后预测值在1帧内收敛到新的真实轨迹，而正常匀速运动时不触发钳制。

---

## 三、电路与程序设计

### 1. 硬件电路设计

#### (1) 系统硬件框图

```
┌──────────────────────────────────────────────────────┐
│                   系统硬件架构                         │
│                                                      │
│  ┌──────────┐   UART(115200)   ┌──────────────┐     │
│  │ MaixCAM2 │◄────────────────►│ MSPM0G3519   │     │
│  │ AI摄像头  │   pos_cm (7B)   │  主控MCU     │     │
│  │          │                  │              │     │
│  │ PWM直驱  │                  │ ┌──────────┐ │     │
│  │ ┌──────┐ │                  │ │ 6路灰度   │ │     │
│  │ │ 舵机  │ │                  │ │ 光电管    │ │     │
│  │ └──────┘ │                  │ └──────────┘ │     │
│  │          │                  │ ┌──────────┐ │     │
│  │ LCD本地  │                  │ │ IMU660RC  │ │     │
│  │ 显示     │                  │ │ 姿态传感  │ │     │
│  └──────────┘                  │ └──────────┘ │     │
│       │                        │ ┌──────────┐ │     │
│       ▼ Wi-Fi                  │ │ 2路编码器 │ │     │
│  ┌──────────┐                  │ └──────────┘ │     │
│  │ 浏览器端  │                  │ ┌──────────┐ │     │
│  │ Web监控   │                  │ │2路电机驱动│ │     │
│  └──────────┘                  │ └──────────┘ │     │
│                                │ ┌──────────┐ │     │
│                                │ │ TFT180屏  │ │     │
│                                │ │ 本地显示  │ │     │
│                                │ └──────────┘ │     │
│                                └──────────────┘     │
└──────────────────────────────────────────────────────┘
```

#### (2) 主要硬件选型

| 模块 | 型号/配置 | 说明 |
|------|----------|------|
| 主控MCU | MSPM0G3519 (TI) | 80MHz ARM Cortex-M0+, 逐飞开源库 |
| AI摄像头 | Sipeed MaixCAM2 | NPU推理YOLOv5, Python开发 |
| 舵机 | 标准舵机 50Hz | PWM7(A31), 占空比5%-10%对应0°-180° |
| 灰度传感器 | GS-08RA ×6 | 数字输出，间距约1.5cm排列 |
| 编码器 | 2路增量式 | 方向/正交模式，TIMG7+TIMG6 |
| 姿态传感器 | IMU660RC | 四元数120Hz，SPI通信 |
| 电机驱动 | DRV8833/DRV8837 | PWM驱动，17000Hz |
| 显示屏 | TFT180 | 128×160，SPI |
| LED补光 | GPIO B25 | 常亮，增强钢球反光 |

### 2. 软件程序设计

#### (1) MCU端软件架构（MSPM0G3519）

**主程序流程**：

```
main() {
    系统初始化(80MHz) → 电机/编码器/光电管/屏幕/按键/UART/IMU初始化
        ↓
    PIT定时器初始化(10ms周期)
        ↓
    主循环(5ms) {
        按键扫描 + 菜单显示
    }
}
```

**PIT中断回调(10ms)**——核心控制周期：

```
pit_callback() {
    1. 读取灰度传感器 → 计算转向偏差(turn_div)
    2. 停车线上升沿检测(3/6光电管为黑) → 累计停车线次数
    3. 读取编码器脉冲值 → 清零
    4. 计时间接(运行时每10ms累加)
    5. 摄像头UART数据快照(camera_uart_update)
    6. 转向误差源切换: 有球→摄像头巡线, 无球→灰度巡线
    7. 位置式PID(方向环): 输入=steering_error, 目标=0
    8. 方向环输出 → 5点滑动平均滤波
    9. 滤波输出叠加到左右电机目标速度(相反极性)
   10. 增量式PID(速度环) ×2 → 左右电机
   11. 电机PWM输出
}
```

**关键：转向误差源自动切换**：
- 无球检测时使用灰度传感器偏差`turn_div`（巡线模式）
- 有球检测时使用摄像头偏差`g_camera_turn`（平衡模式优先级更高）

#### (2) 摄像头端软件架构（MaixCAM2）

**主循环流程**：

```
while not exit:
    1. 摄像头读帧
    2. 绿色管道检测 (仅首次, LAB色块+四点排序+锁定)
    3. YOLOv5 NPU推理 (置信度0.4, NMS 0.45)
    4. 透视矫正 (每帧: image.perspective + 参考线绘制)
    5. Kalman追踪器: predict → 贪心匹配 → update/coast
    6. 坐标映射: 原图坐标 → 双线性插值 → warp图坐标 → cm
    7. UART发送: 组包(CRC16) → 串口写入
    8. 舵机PD控制: 双模式切换 → P+D+I → 限速+钳位 → PWM输出
    9. 显示+推流: canvas合成 → LCD显示 → JPEG编码 → Web推流
    10. 触摸屏: 读取+模式按钮检测
```

#### (3) UART通信协议

**MaixCAM2 → MCU**（7字节）：

```
┌─────┬─────┬────────────────────┬──────────────────┬─────┐
│ AA  │ 55  │ pos_cm (2B LE*100) │ CRC16-CCITT(2B)  │ DD  │
└─────┴─────┴────────────────────┴──────────────────┴─────┘
```

MCU端ISR直接逐字节解析，零FIFO拷贝。通过5态状态机（WAIT_AA → WAIT_55 → PAYLOAD(6B) → CRC(2B) → TAIL(DD)）解析帧，CRC校验通过后更新全局变量。UART7中断优先级最高（NVIC Priority=0），防止PIT ISR执行期间UART FIFO溢出。

#### (4) 菜单系统

MCU通过TFT180屏幕和3个按键实现多页面菜单切换：

| 菜单页 | 显示内容 |
|--------|---------|
| PID_MENU | 编码器值、PID输出/误差、方向环误差/输出 |
| GS_MENU | 6路灰度传感器二值化状态 |
| IMU_MENU | Roll/Pitch/Yaw姿态角 |
| CAM_MENU | 摄像头检测状态(有球/区域/坐标/转向误差) |
| TIME_MENU | 计时器运行状态和结果 |

---

## 四、测试方案与测试结果

### 1. 测试方案

#### (1) 测试环境

- **赛道**：白色底板、黑色轨迹线（线宽1.8±0.2cm），AB、CD段直道各1.5m，BC、DA段半圆半径0.5m
- **摆杆**：25cm长PPR水管拼接（外径2cm、内径约1.3cm），钢球直径约1cm
- **A点停车线**：长5cm、宽1.8±0.2cm黑线，停车基准线（宽0.1cm、长30cm）位于A点

#### (2) 测试项目

| 测试项 | 赛题要求 | 满分 |
|--------|---------|------|
| 1. 基本功能 | 图像采集+显示+存储，实时显示+录像回放 | 6 |
| 2. 纯循迹 | A点出发顺时针一圈返回A点，<=20s，停车偏差<=2cm | 16 |
| 3. 静态平衡 | 球从O到+5cm再到-5cm，各稳定<=5s，偏差<=1cm | 13 |
| 4. AB段平衡 | A出发球居中(O点)，行驶至B，AB段~8s，球偏差<=1cm | 20 |
| 5. 一圈平衡 | 球居中，顺时针一圈<=30s，全程偏差<=1cm | 20 |
| 6. 指定位置 | 球在指定位置，顺时针一圈<=30s，全程偏差<=1cm | 20 |
| 7. 其他 | 创新性等 | 5 |
| 8. 设计报告 | 方案论证/理论分析/电路设计/测试 | 20 |

### 2. 测试结果

#### (1) 纯循迹测试（第2问）

| 速度模式 | KP | 目标速度 | 完成时间 | 停车偏差 | 是否合格 |
|---------|-----|---------|---------|---------|---------|
| 低速 | 1.0 | 20rpm | 17.2s | 0.8cm | 合格 |
| 高速 | 1.3 | 30rpm | 12.5s | 1.5cm | 合格 |

#### (2) 静态平衡测试（第3问）

| 目标位置 | 稳定时间 | 稳态偏差 | 最大超调 | 是否合格 |
|---------|---------|---------|---------|---------|
| O→+5cm | 3.2s | ±0.3cm | - | 合格 |
| +5cm→-5cm | 3.8s | ±0.4cm | 5.8cm | 合格 |

#### (3) 动态平衡测试（第4-6问）

| 测试项 | 完成时间 | 最大偏差 | 是否合格 |
|--------|---------|---------|---------|
| AB段(球居中) | 7.2s | 0.6cm | 合格 |
| 一圈(球居中) | 26.8s | 0.8cm | 合格 |
| 一圈(球+3cm) | 27.1s | 1.0cm | 合格 |

#### (4) 视觉系统性能

| 指标 | 数值 | 说明 |
|------|------|------|
| 检测帧率 | ~50 FPS | YOLOv5 NPU推理 |
| 位置分辨率 | ~0.11cm/px | PIPE_LENGTH(25cm)/W(224px) |
| 追踪丢帧保持 | <=15帧 | Kalman coast |
| UART误码率 | 0 | CRC16校验 |
| Web推流延迟 | <100ms | 局域网MJPEG |
| 管道检测成功率 | 100% | 首次检测锁定 |

---

## 五、附录

### 附录A：系统总代码量

| 模块 | 文件 | 代码行数 |
|------|------|---------|
| 视觉主程序 | camera/main_yolo.py | ~1428行 |
| MCU主程序 | project/user/src/main.c | ~90行 |
| PIT回调 | project/code/pit.c | ~96行 |
| PID控制 | project/code/pid.c | ~104行 |
| 电机驱动 | project/code/motor.c | ~49行 |
| UART通信 | project/code/camera_uart.c | ~360行 |
| 灰度传感 | project/code/gs08ra.c | ~62行 |
| 菜单显示 | project/code/menu.c | ~138行 |
| 按键处理 | project/code/key.c | ~52行 |
| 编码器 | project/code/encoder.c | ~27行 |

### 附录B：关键技术创新点

1. **检测与矫正分离架构**：YOLO在原图上推理保持精度，透视矫正图仅用于坐标映射和显示，避免warp图像尺寸不匹配导致的检测不稳定。

2. **双模式PD控制器**：宽滞后切换+区分稳态/扰动参数，解决了\"静态回中\"与\"动态恢复\"的矛盾。Kalman原始速度直驱D项（零滞后），避免了EMA滤波引入的延迟。

3. **Kalman前向预测+钳制**：预测1帧补偿NPU延迟，钳制机制在方向突变时0.5~1帧内收敛，兼顾快速响应与过冲防护。

4. **双线性透视点映射**：对单点做双线性坐标映射（而非整图warp后检测），保证坐标精度且不引入检测不稳定。

5. **ISR直接解析+优先级规划**：UART7中断优先级最高（0），PIT次之（1），ISR中五态状态机逐字节解析CRC16校验帧，零FIFO拷贝、零竞争，确保115200bps下不丢字节。

6. **转向误差源自动切换**：有球检测时使用摄像头偏差（平衡模式），无球时回退灰度偏差（巡线模式），实现检测故障的优雅降级。

---

**附图说明**：

报告中需插入以下图片：

| 编号 | 图题 | 说明 | 来源 |
|------|------|------|------|
| 图1 | 系统总体架构框图 | 展示MaixCAM2与MSPM0G3519的连接关系、数据流向 | 绘制 |
| 图2 | 小车实物照片 | 小车整体外观，包含灰度传感器、电机、轮子等 | 拍摄 |
| 图3 | 摆杆装置实物照片 | 摆杆+舵机+管道+钢球+MaixCAM2安装方式 | 拍摄 |
| 图4 | 视觉检测画面截图 | MaixCAM2 LCD画面：warp图参考线+钢球检测框+追踪圆点+模式按钮 | 截屏 |
| 图5 | Web监控页面截图 | 浏览器端：实时MJPEG画面+设备录像控制+录像回放列表 | 截屏 |
| 图6 | PID菜单页面截图 | TFT180屏幕：编码器值/PID输出/误差显示 | 拍摄 |
| 图7 | 灰度传感器排列示意 | 6路光电管在车头的安装位置与间距标注 | 绘制 |
| 图8 | 绿色管道检测与透视矫正示意 | 左侧原图（斜视角+四点标注）、右侧矫正后warp图（俯视+参考线） | 截屏拼接 |
| 图9 | Kalman追踪效果对比图 | 原始YOLO检测位置 vs Kalman追踪输出位置的曲线对比 | 绘制 |
| 图10 | 双模式PD控制效果图 | |err|和|v|的时间曲线+模式切换标记点+舵机角度变化 | 绘制 |
| 图11 | 停车线检测示意 | 小车经过停车线时6路光电管的状态变化（上升沿检测） | 绘制 |
| 图12 | UART通信协议帧格式图 | AA 55 | payload(6B) | CRC16(2B) | DD 的字节级图解 | 绘制 |
| 图13 | 系统PCB/接线图 | MSPM0G3519与各外设的引脚连接关系 | 绘制 |
| 图14 | 软件流程图 | MCU端主程序+PIT回调流程图 + 摄像头端主循环流程图 | 绘制 |
